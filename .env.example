# Copie este arquivo para ".env" (não versionado — veja .gitignore) e
# preencha com as credenciais reais do ambiente de cada integrante.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=troque_esta_senha
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=desafio_dados

MONGODB_USER=
MONGODB_PASSWORD=
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=desafio_dados
